<van-tabbar id="tabbar" active="{{ active }}" bind:change="onChange" active-color="orange">
    <van-tabbar-item wx:for="{{ list }}" wx:key="index" icon="{{ item.icon }}">{{item.text}}</van-tabbar-item>
</van-tabbar>